package main

import (
    "fmt"
)

func main() {
    // Example of using Go's built-in types and structures
    var age int = 18
    var name string = "John Doe"

    fmt.Printf("Hello, %s!\n", name)
}
